name: stress-robustness-targeted

on:
  push:
    branches:
      - codex/afuture-stress-robustness-20260823
  pull_request:
    branches:
      - main

jobs:
  targeted:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - name: Targeted directional robustness tests
        run: >-
          python -m pytest -q
          tests/test_directional_portfolio.py
          tests/test_directional_acceptance.py
          tests/test_directional_acceptance_simulation.py
          tests/test_execution_aligned_policy.py
          tests/test_execution_aligned_runtime.py
          tests/test_directional_stress_robustness.py
      - run: python -m compileall -q afuture
